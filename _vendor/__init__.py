"""Vendored third-party code bundled with lerobot_sdk.

This package contains a pruned, self-contained copy of the subset of the
official ``lerobot`` codebase that the SDK depends on at runtime, rewritten to
live under the private ``lerobot_sdk._vendor.lerobot_core`` namespace. It exists
so that ``lerobot_sdk`` is independent of any external ``lerobot`` installation
or in-repo source tree.
"""
